{
  "name": "manjam-admin-legacy-compat",
  "version": "1.0.0",
  "private": true,
  "description": "Recoverable mirror of the legacy MANJAM Admin with an MFA-compatible login gateway.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:e2e": "node test/e2e-mfa-fixture.mjs",
    "verify": "node --check public/mfa-compat.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
